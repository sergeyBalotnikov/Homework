<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CulcButtonOperationStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">24sp</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:background">@drawable/drawable_culc_shape_operation</item>
        <item name="android:textColor">#ffffff</item>
    </style>
    <style name="CulcButtonNumberStyle" parent="CulcButtonOperationStyle">
        <item name="android:background">@drawable/drawable_culc_shape_number</item>
    </style>

</resources>
